"""
Custom high-level robot actions live here.

Each action module should expose a `run(api, runtime, **kwargs)` function
that returns a `voice.control.task_structures.ExecutionResult` or a dict with
`status/node/reason/evidence`. Modules can use `api` (RobotAPI) to invoke
navigation/perception/manipulation primitives.
"""
