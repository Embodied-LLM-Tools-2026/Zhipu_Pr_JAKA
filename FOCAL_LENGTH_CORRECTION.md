"""
================================================================================
                        焦距重新计算（修正）
================================================================================

问题发现：
  你说"图片被 resize 到了 640×480，然后又 resize 到了 1000×1000"
  但焦距 fx=1173 与预期不符
  
  经过反推验证：
  - 如果用 fx=1173 和 FOV=80°，反推出的 FOV 只有 46.2°，不对
  - 这说明你的焦距或 resize 过程可能不同

================================================================================


█ 两种可能的情况
════════════════════════════════════════════════════════════════════════════

【情况 1】你的 resize 过程是这样的：

  原始 2K (2560×1440)
       ↓ (非均匀 resize 保持宽高比)
  640×480 (4:3 比例)
       ↓ (拉伸到正方形)
  1000×1000 (宽高拉伸比例不同)
  
  这种情况下，焦距应该是：
    fx_1000 = fx_2560 × (1000/2560) = 1525 × 0.391 = 596 px
  
  ❌ 但你说的是 1173，不符合


【情况 2】实际情况可能是：

  也许你在 1000×1000 的图上直接使用原始焦距？
  或者你的摄像头参数不同？

================================================================================


█ 正确的焦距计算方法
════════════════════════════════════════════════════════════════════════════

请按照这个步骤重新验证：

【步骤 1】确认你的原始摄像头焦距

选择下面最符合你情况的：

  A. 你知道原始 2560×1440 分辨率下的焦距（单位像素）
     fx_original = _____ px
  
  B. 你只知道 FOV 和原始分辨率
     FOV = 80°
     Width = 2560 px
     
     则：fx_original = Width / (2 × tan(FOV/2))
                     = 2560 / (2 × tan(40°))
                     = 2560 / (2 × 0.8391)
                     = 1525 px

  C. 你已知在 1000×1000 上的焦距
     fx_1000 = _____ px
     
     则：fx_original = fx_1000 × (2560 / 1000)
                     = fx_1000 × 2.56


【步骤 2】确认你的 resize 方式

确认以下哪种是你的 resize 流程：

  方式 1（均匀缩放）：
    2560×1440 → 直接 resize 到 1000×1000
    缩放系数：1000/2560 = 0.391
    fx_1000 = 1525 × 0.391 = 596 px
  
  方式 2（保留宽度，调整高度）：
    2560×1440 → resize 到 1000×562.5 → 然后 pad/crop 到 1000×1000
    宽度缩放系数：1000/2560 = 0.391
    fx_1000 = 1525 × 0.391 = 596 px
  
  方式 3（通过 640×480 中间过程）：
    2560×1440 → 640×480 (宽度 2560→640, 系数 0.25)
                        → 1000×1000 (宽度 640→1000, 系数 1.5625)
    综合缩放：1000/2560 = 0.391
    fx_1000 = 1525 × 0.391 = 596 px
  
  方式 4（其他特殊处理）：
    请描述你的具体 resize 代码

都是同样的结果：fx_1000 应该是 596 px 左右


【步骤 3】查看 VLM.py 中的 resize 代码

你之前提到的 _prepare_image_for_vlm 函数中的 resize：

    if target and tuple(original_size) != target:
        resized_img = img.resize(target, Image.BILINEAR)
    
    这里 target = (1000, 1000)，所以是均匀拉伸到正方形

================================================================================


█ 推荐的焦距值
════════════════════════════════════════════════════════════════════════════

根据你的摄像头参数（2K 80°），正确的焦距应该是：

  ✅ 原始焦距 (2560×1440): fx ≈ 1525 px
  ✅ 1000×1000 焦距: fx ≈ 596 px
  
  你提的 1173 可能来自其他计算，不是 1000×1000 的

如果一定要用 1173，那应该对应的 FOV 是：
  
  FOV = 2 × arctan(1000 / (2 × 1173)) = 46.2°
  
  这不符合你的 80° FOV


════════════════════════════════════════════════════════════════════════════


█ 行动方案（三选一）
════════════════════════════════════════════════════════════════════════════

【方案 A】使用计算出的焦距 596 px（推荐）

  修改 VLM.py：
  
    self.camera_fx = 596   # ← 改这个
    self.camera_fy = 596
    self.camera_fov_h_deg = 80.0


【方案 B】如果你确信用 1173，则 FOV 应该是 46.2°

  修改 VLM.py：
  
    self.camera_fx = 1173
    self.camera_fy = 1173
    self.camera_fov_h_deg = 46.2  # ← 改这个


【方案 C】直接用 FOV 公式（不用焦距）

  在 control_chassis_to_center 中改为：
  
    turn_angle = dx_pixels * (math.radians(80) / 1000)
    
  这样就不用管焦距了，直接用 FOV


════════════════════════════════════════════════════════════════════════════


█ 快速验证方法
════════════════════════════════════════════════════════════════════════════

运行这个代码确认哪个焦距是对的：

    import math
    
    # 你的参数
    original_fov_deg = 80.0
    original_width = 2560
    target_width = 1000
    
    # 计算焦距
    fx_original = original_width / (2 * math.tan(math.radians(original_fov_deg/2)))
    fx_target = fx_original * (target_width / original_width)
    
    print(f"原始焦距 (2560×1440): {fx_original:.0f} px")
    print(f"目标焦距 (1000×1000): {fx_target:.0f} px")
    
    # 反推验证
    fov_check = 2 * math.degrees(math.atan(target_width / (2 * fx_target)))
    print(f"用计算的焦距反推 FOV: {fov_check:.1f}° (应该是 80°)")


════════════════════════════════════════════════════════════════════════════


█ 建议
════════════════════════════════════════════════════════════════════════════

1. 请告诉我：
   • 你的 1173 这个数字是怎么来的？
   • 是从规格书查到的吗？
   • 还是其他计算方式？

2. 根据标准计算，应该是 fx=596 px（对应 1000×1000）

3. 如果有疑问，可以用 FOV 公式（更直观）：
   turn_angle = dx_pixels × (FOV_radians / image_width)
   
   这样不需要焦距参数

4. 最准确的方法还是标定你的摄像头（30 分钟）
   用 OpenCV 标定得到准确的 fx, fy, cx, cy, distortion 等参数

"""

if __name__ == "__main__":
    print(__doc__)
    
    # 自动计算
    import math
    
    print("\n" + "="*70)
    print("自动计算你的摄像头焦距")
    print("="*70)
    
    fov_deg = 80.0
    original_width = 2560
    target_width = 1000
    
    fx_original = original_width / (2 * math.tan(math.radians(fov_deg/2)))
    fx_target = fx_original * (target_width / original_width)
    
    print(f"\n输入参数:")
    print(f"  原始分辨率: {original_width}×1440 像素")
    print(f"  视场角: {fov_deg}°")
    print(f"  目标分辨率: {target_width}×{target_width} 像素")
    
    print(f"\n计算结果:")
    print(f"  原始焦距 (2560×1440): {fx_original:.0f} px")
    print(f"  缩放系数: {target_width / original_width:.4f}")
    print(f"  目标焦距 (1000×1000): {fx_target:.0f} px")
    
    # 反推验证
    fov_check = 2 * math.degrees(math.atan(target_width / (2 * fx_target)))
    print(f"\n✓ 反推验证:")
    print(f"  用 fx={fx_target:.0f} 反推 FOV: {fov_check:.1f}°")
    print(f"  原始 FOV: {fov_deg}°")
    print(f"  ✓ 验证通过！")
    
    print(f"\n推荐在 VLM.py 中使用：")
    print(f"  self.camera_fx = {fx_target:.0f}")
    print(f"  self.camera_fy = {fx_target:.0f}")
    print(f"  self.camera_fov_h_deg = {fov_deg}")
    print("\n")
